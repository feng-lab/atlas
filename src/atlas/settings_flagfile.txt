# comma-separated list of flags for llfio mapped file handle, default is multiplexable, possible values are:
# none,unlink_on_first_close,disable_safety_barriers,disable_safety_unlinks,disable_prefetching,
# maximum_prefetching,win_disable_unlink_emulation,win_disable_sparse_file_creation,disable_parallelism,
# win_create_case_sensitive_directory,multiplexable,byte_lock_insanity,anonymous_inode
--zimg_llfio_mapped_file_handle_flags=multiplexable

# Proportion of RAM that will be used for image cache, default is 0.3
--atlas_image_cache_memory_proportion=0.3

# Proportion of RAM that will be used for image region cache, default is 0.3
--atlas_image_region_cache_memory_proportion=0.3

# Proportion of RAM that will be used for Neuroglancer precomputed chunk cache (per opened volume), default is 0.3
--atlas_ng_precomputed_chunk_cache_memory_proportion=0.3

# Proportion of RAM that will be used for Neuroglancer precomputed sharded minishard index cache (per opened volume), default is 0.05
--atlas_ng_precomputed_minishard_index_cache_memory_proportion=0.05

# HTTP / Network settings for remote datasets (Neuroglancer, etc.)

# Path to a PEM CA bundle for HTTPS requests (empty = auto-detect; also respects env SSL_CERT_FILE).
--atlas_http_ca_bundle=

# Outbound HTTP proxy strategy using OS system proxy settings only (no proxy URL flags).
# Values: auto (alternate direct/proxy between retries), no_proxy (always direct),
#         proxy_if_available (always use system proxy if one exists for the URL).
--atlas_http_proxy_strategy=auto

# Maximum number of retries for transient network/handshake errors in HTTP GET, default is 3.
--atlas_http_max_retries=3

# Backoff delay in milliseconds before retrying transient HTTP errors, default is 200ms.
--atlas_http_retry_backoff_initial_ms=200

# Maximum backoff delay in milliseconds for transient HTTP error retries, default is 2000ms.
--atlas_http_retry_backoff_max_ms=2000

# Persistent on-disk cache for HTTP GET results (helps avoid re-downloading across sessions).
# Max size in bytes (default 20 GiB). Set to 0 to disable.
--atlas_disk_cache_http_max_bytes=21474836480

# Persistent on-disk cache for 3D image-region blocks read via ZImgPack/ZImgRegionCache.
# Max size in bytes (default 20 GiB). Set to 0 to disable.
--atlas_disk_cache_imgregion_max_bytes=21474836480

# Persistent on-disk cache for downsampled 3D preview volumes used by Z3DImg::readVolumes().
# Max size in bytes (default 5 GiB). Set to 0 to disable.
--atlas_disk_cache_imgpreview_max_bytes=5368709120
# Async write queue budget in bytes (default 1024 MiB; min 256 MiB). When full, writes are dropped (best-effort).
--atlas_disk_cache_imgpreview_async_max_pending_bytes=1073741824

# Root directory for the Atlas persistent disk cache (empty = auto-chosen Atlas cache/config directory).
--atlas_disk_cache_dir=

# Interval in seconds for logging folly global executor status during waiting, default is 5
--atlas_log_folly_global_executor_status_interval_in_seconds=5

# Whether readRegionToImg uses multithreaded resize, default is false
--atlas_readRegionToImg_use_multithreaded_resize=false

# Use PBO when number of blocks to upload is larger than this threshold, default is 0
--atlas_number_of_blocks_to_use_PBO_threashold=0

# Number of threads fft will use, default is 0 which is hardware concurrency.
--zimg_global_fft_number_of_threads=0

# Whether to use mkl for fft computation if available, default is true
--zimg_use_mkl_for_fft_if_available=true

# Whether to use fftw for fft computation if available, default is true
--zimg_use_fftw_for_fft_if_available=true

# Whether to create mmap file for nim format, default is false
--zimg_use_mmap_file_for_hdf5=false

# Whether to check openGL error aggressively, default is false, can set to true for debugging
--atlas_debug_opengl=false

# Default 3D rendering backend at startup. Values: opengl, vulkan.
--atlas_default_render_backend=opengl

# Maximum number of rounds for volume rendering, default is 100
--atlas_volume_rendering_maximum_round=100

# Log per-frame 3D image paging stats (resolved blocks/bytes by cache tier, underlying I/O bytes, source decoded bytes, GPU upload bytes), default is false
--atlas_log_3d_paging_frame_stats=false

# Include per-round 3D image paging stats in the per-frame log entry (can be verbose), default is false
--atlas_log_3d_paging_round_stats=false

# Whether to log openGL context switch event, default is false
--atlas_log_glbinding_context_switch=false

# define the width, height, and depth of image blocks in the 3D image cache system, can be 64 (default), 128, 256 or 512
--atlas_image_block_size=64

# Use y-flip in Vulkan final copy shader instead of UI flip
--atlas_vk_copy_yflip_in_shader=true

# Whether to enable debugging features of vulkan, default is false, can set to true for debugging
--atlas_debug_vulkan=false

# set to 1 or higher for verbose logging
--v=0
